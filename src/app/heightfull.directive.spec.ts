import { HeightfullDirective } from './heightfull.directive';

describe('HeightfullDirective', () => {
  it('should create an instance', () => {
    const directive = new HeightfullDirective();
    expect(directive).toBeTruthy();
  });
});
